require 'spec_helper'

describe Conquer::Mpd do
  it 'has a version number' do
    expect(Conquer::Mpd::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
